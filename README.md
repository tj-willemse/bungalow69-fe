# bungalow69-fe

The website for Bungalow 69 in Clifton, Cape Town.

## Development

```bash
npm install
npm run dev
```

Create a local `.env` file from `.env.example` and update `NEXT_PUBLIC_SITE_URL` for the target environment.
